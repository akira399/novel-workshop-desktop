{
  "name": "novel-workshop-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "大肥鱼的小说工坊 — 独立 Windows 桌面版（Electron + React，复用 dsh-novel-writer core）",
  "scripts": {
    "dev": "pnpm --filter @dafuyu/desktop dev",
    "build": "pnpm --filter @dafuyu/desktop build",
    "test": "vitest run",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
